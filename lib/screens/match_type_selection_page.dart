import 'package:flutter/material.dart';

import 'discovery_selection_page.dart';

/// Stable entry point retained for existing routes.
class MatchTypeSelectionPage extends StatelessWidget {
  const MatchTypeSelectionPage({super.key});
  @override
  Widget build(BuildContext context) => const DiscoverySelectionPage();
}
